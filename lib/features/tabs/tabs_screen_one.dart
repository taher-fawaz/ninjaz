import 'package:flutter/material.dart';

class TabScreenOne extends StatelessWidget {
  const TabScreenOne({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tab Screen One'),
    );
  }
}
