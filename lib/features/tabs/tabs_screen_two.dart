import 'package:flutter/material.dart';

class TabScreenTwo extends StatelessWidget {
  const TabScreenTwo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tab Screen Two'),
    );
  }
}
